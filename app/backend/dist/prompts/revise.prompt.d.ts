/**
 * System prompt for POST /revise — apply user instruction to existing roadmap.
 */
export declare function buildRevisePrompt(currentRoadmap: string, instruction: string): {
    system: string;
    user: string;
};
//# sourceMappingURL=revise.prompt.d.ts.map